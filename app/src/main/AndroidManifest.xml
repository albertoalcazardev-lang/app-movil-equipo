<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso para poder hacer peticiones HTTP a FakeStoreAPI -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permiso para poder PREGUNTAR si hay conexión (US01 - Escenario 3) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/nombre_app"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Autenticacion">

        <!-- Pantalla de Login: es la puerta de entrada de la app.
             android:exported="true" es obligatorio en apps recientes
             para cualquier Activity con intent-filter LAUNCHER. -->
        <activity
            android:name=".pantallas.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla "Mi cuenta" / principal temporal, a la que se
             llega SOLO después de iniciar sesión correctamente. -->
        <activity
            android:name=".pantallas.PrincipalActivity"
            android:exported="false" />

    </application>
</manifest>